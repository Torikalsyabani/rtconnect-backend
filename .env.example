# ── Lokal (SQLite) ──────────────────────────────────────────
PORT=5000
FLASK_DEBUG=true
DB_PATH=rtconnect.db
SECRET_KEY=rtconnect_secret_GANTI_INI_DI_PRODUCTION

# ── Production PostgreSQL (Render/Railway/Supabase) ─────────
# Uncomment baris ini dan isi dengan URL dari provider:
# DATABASE_URL=postgresql://postgres:torikal210@localhost:5432/rtconnect
# SECRET_KEY=torikalsyabaniganteng

# ── Cloudinary (upload foto) ────────────────────────────────
# CLOUDINARY_URL=cloudinary://API_KEY:API_SECRET@CLOUD_NAME
